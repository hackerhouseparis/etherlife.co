import React from 'react'

import Button from './Button'

const styleLogo = {
  padding: '5% 0 3% 0',
}

const styleSubTitle = {
  fontFamily: "Roboto, sans-serif",
  fontSize: "60px",
}

export default () => (
  <div>
  </div>
)
